services:
  notification-service:
    build: .
    ports:
      - "8082:8082"
    environment:
      KAFKA_ENABLED: true
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
networks:
  default:
    external: true
    name: kafka-net